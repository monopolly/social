package social

func Facebook(token string) (u User, err error) {
	/* link := fmt.Sprintf("https://graph.facebook.com/me?fields=id,name,email&access_token=%s", token)
	b, err := file.Downloads(link)
	if err != nil {
		return
	}
	u.Key = jsons.String(b, "id")
	u.Email = jsons.String(b, "email")
	u.Name = jsons.String(b, "name") */
	return
}
