package social

//testing

import (
	"fmt"
	"runtime"
	"strings"
	"testing"

	"github.com/BillSJC/appleLogin"
	"github.com/stretchr/testify/assert"
)

func Test3(ggggg *testing.T) {
	function, _, _, _ := runtime.Caller(0)
	fn := runtime.FuncForPC(function).Name()
	fn = fn[strings.LastIndex(fn, ".Test")+5:]
	fn = strings.Join(strings.Split(fn, "_"), ": ")
	fmt.Printf("\033[1;32m%s\033[0m\n", fn)

	a := assert.New(ggggg)
	_ = a
	config := appleLogin.InitAppleConfig(teamID, clientID, callback, keyID)
	config.LoadP8CertByByte([]byte(privateKey))
	tt, err := config.GetAppleToken(appletoken, 3600)
	if err != nil {
		fmt.Println(err)
		return
	}
	fmt.Println(tt.AccessToken)

}
